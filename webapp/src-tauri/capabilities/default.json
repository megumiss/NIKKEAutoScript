{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "No JavaScript-side native API is exposed to the backend-hosted SPA.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
