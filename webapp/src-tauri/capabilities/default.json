{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only the window controls used by the in-page custom titlebar are exposed; the backend-hosted SPA loads from a remote origin, so the capability must extend to it explicitly. The origin host comes from WebuiHost in deploy.yaml and is not known at build time, so any http origin is allowed here; the navigation guard in main.rs still restricts the webview to the configured host and port.",
  "remote": { "urls": ["http://*:*"] },
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-is-maximized",
    "core:window:allow-hide",
    "core:window:allow-close",
    "allow-desktop-update-status",
    "allow-desktop-update-check",
    "allow-desktop-update-apply"
  ]
}
