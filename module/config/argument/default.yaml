# ==================== Reward ====================

Reward:
  Scheduler:
    Enable: true

Notify:
  Scheduler:
    Enable: true

DailyRecruit:
  Scheduler:
    Enable: false

Commission:
  Scheduler:
    Enable: false

RookieArena:
  Scheduler:
    Enable: false

SpecialArena:
  Scheduler:
    Enable: false

SpecialArenaWatch:
  Scheduler:
    Enable: false

ChampionArena:
  Scheduler:
    Enable: false

Conversation:
  Scheduler:
    Enable: false

SimulationRoom:
  Scheduler:
    Enable: false

TribeTower:
  Scheduler:
    Enable: false

Daily:
  Scheduler:
    Enable: false

RubbishShop:
  Scheduler:
    Enable: false

MissionPass:
  Scheduler:
    Enable: false

Liberation:
  Scheduler:
    Enable: false

DailyGift:
  Scheduler:
    Enable: false

WeeklyGift:
  Scheduler:
    Enable: false

MonthlyGift:
  Scheduler:
    Enable: false

WarehouseStats:
  Scheduler:
    Enable: false

BlaDaily:
  Scheduler:
    Enable: false

BlaCDK:
  Scheduler:
    Enable: false

BlaExchange:
  Scheduler:
    Enable: false

Event:
  Scheduler:
    Enable: false

Event2:
  Scheduler:
    Enable: false

Coop:
  Scheduler:
    Enable: false

SoloRaid:
  Scheduler:
    Enable: false

UnionRaid:
  Scheduler:
    Enable: false

# ==================== 周/月任务默认周期 ====================

Synchro:
  Scheduler:
    Enable: false
    Cadence: weekly

Recycling:
  Scheduler:
    Enable: false
    Cadence: weekly

OutpostStory:
  Scheduler:
    Cadence: weekly

EpisodeViewing:
  Scheduler:
    Cadence: weekly

WeeklyGift:
  Scheduler:
    Cadence: weekly

StepUpGift:
  Scheduler:
    Cadence: weekly

RubbishShop:
  Scheduler:
    Cadence: weekly
    # 周二+周五双档，统一到单天会改变既有行为
    WeeklyDay: '2, 5'

MonthlyGift:
  Scheduler:
    Cadence: monthly

BlaExchange:
  Scheduler:
    Cadence: monthly
